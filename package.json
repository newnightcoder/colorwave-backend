{
  "name": "colorwave",
  "type": "module",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.2",
    "nanoid": "^3.1.30",
    "nodemon": "^2.0.15",
    "stripe": "^8.195.0"
  },
  "scripts": {
    "start": "node app",
    "dev": "cd client && yarn build && cd ../../backend-shop && nodemon app",
    "heroku-postbuild": "cd client && yarn && yarn build"
  },
  "engines": {
    "node": "14.17.6"
  }
}
